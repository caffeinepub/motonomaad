# Specification

## Summary
**Goal:** Update the landing page hero headline typography to use the “Black Ops One” font and visually emphasize the word “FREE” in the primary orange color.

**Planned changes:**
- Update the hero headline on `frontend/src/pages/LandingPage.tsx` to render “RIDE FREE. RIDE FAR.” using the “Black Ops One” typeface.
- Style only the word “FREE” to be filled (non-transparent) with the existing primary theme orange token, while keeping the rest of the headline readable and consistent with the current hero layout.

**User-visible outcome:** On the home page ("/"), the hero headline displays in the Black Ops One font, with “FREE” clearly filled in the primary orange color, and all other visible text remains unchanged.
